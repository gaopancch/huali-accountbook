# 邮件发送功能配置指南

本文档详细说明如何配置和部署记账宝应用的邮件发送功能。

## 目录
1. [获取163邮箱SMTP授权码](#获取163邮箱smtp授权码)
2. [部署Supabase Edge Function](#部署supabase-edge-function)
3. [配置环境变量](#配置环境变量)
4. [测试邮件发送功能](#测试邮件发送功能)
5. [手机号到运营商邮箱映射](#手机号到运营商邮箱映射)
6. [常见问题](#常见问题)

---

## 获取163邮箱SMTP授权码

### 步骤说明

1. **登录163邮箱**
   - 访问 [mail.163.com](https://mail.163.com)
   - 使用您的账号 `gaopancch@163.com` 登录

2. **进入设置**
   - 点击页面右上角的 **"设置"** 按钮
   - 在下拉菜单中选择 **"POP3/SMTP/IMAP"**

3. **开启SMTP服务**
   - 找到 **"SMTP服务"** 选项
   - 点击 **"开启"** 按钮
   - 系统会要求进行身份验证(通常是手机短信验证)

4. **获取授权码**
   - 验证成功后，系统会生成一个 **授权码**
   - 这是一个16位的随机字符串，例如：`ABCD1234EFGH5678`
   - **重要**: 请妥善保存此授权码，它将用于程序中的SMTP密码

5. **注意事项**
   - 授权码不是您的邮箱登录密码
   - 授权码只显示一次，请立即保存
   - 如果忘记，需要重新生成

---

## 部署Supabase Edge Function

### 前置要求

1. **安装Supabase CLI**
   ```bash
   npm install -g supabase
   ```

2. **登录Supabase**
   ```bash
   supabase login
   ```

### 部署步骤

1. **初始化Supabase项目(如果还没有初始化)**
   ```bash
   cd /Users/gaopan/Downloads/huali-accountbook
   supabase init
   ```

2. **关联到您的Supabase项目**
   ```bash
   supabase link --project-ref your-project-ref
   ```
   > 您可以在Supabase项目设置中找到 `project-ref`

3. **部署Edge Function**
   ```bash
   supabase functions deploy send-reset-password-email
   ```

4. **验证部署**
   ```bash
   supabase functions list
   ```
   您应该能看到 `send-reset-password-email` 函数已部署

---

## 配置环境变量

### 在Supabase Dashboard中配置

1. **访问Supabase Dashboard**
   - 登录 [app.supabase.com](https://app.supabase.com)
   - 选择您的项目

2. **进入Edge Functions设置**
   - 左侧菜单选择 **"Edge Functions"**
   - 点击 **"Settings"** 或 **"Secrets"**

3. **添加环境变量**

   添加以下环境变量：

   | 变量名 | 值 | 说明 |
   |--------|-----|------|
   | `SMTP_HOST` | `smtp.163.com` | 163邮箱SMTP服务器地址 |
   | `SMTP_PORT` | `465` | SMTP端口(使用SSL) |
   | `SMTP_USER` | `gaopancch@163.com` | 您的163邮箱地址 |
   | `SMTP_PASSWORD` | `您的授权码` | 步骤1中获取的授权码 |

4. **保存配置**
   - 点击保存按钮
   - 环境变量会立即生效

### 使用CLI配置(可选)

```bash
# 设置SMTP配置
supabase secrets set SMTP_HOST=smtp.163.com
supabase secrets set SMTP_PORT=465
supabase secrets set SMTP_USER=gaopancch@163.com
supabase secrets set SMTP_PASSWORD=您的授权码
```

---

## 测试邮件发送功能

### 测试邮箱找回

1. **打开应用的忘记密码页面**
   ```
   http://localhost:3000/forgot-password
   ```

2. **选择"邮箱找回"**

3. **输入一个已注册的邮箱地址**

4. **点击"重置密码"按钮**

5. **检查结果**
   - 如果配置正确，您应该看到提示：`密码重置成功！临时密码已发送到您的邮箱 xxx@xxx.com，请查收。`
   - 检查邮箱收件箱(如果没收到，检查垃圾邮件文件夹)
   - 如果邮件发送失败，页面会显示临时密码作为备用方案

### 测试手机号找回

1. **选择"手机找回"**

2. **输入一个已注册的手机号**
   - 例如：13812345678 (移动)
   - 例如：18912345678 (电信)

3. **点击"重置密码"按钮**

4. **检查结果**
   - 系统会自动将邮件发送到对应的运营商邮箱
   - 移动用户: 13812345678@139.com
   - 电信用户: 18912345678@189.cn
   - 联通用户: 13112345678@wo.cn

---

## 手机号到运营商邮箱映射

Edge Function会自动识别手机号运营商并转换为对应的邮箱地址。

### 支持的运营商

| 运营商 | 邮箱域名 | 示例 |
|--------|----------|------|
| 中国移动 | `139.com` | 13812345678@139.com |
| 中国电信 | `189.cn` | 18912345678@189.cn |
| 中国联通 | `wo.cn` | 13112345678@wo.cn |

### 号段识别

**移动号段**
- 134, 135, 136, 137, 138, 139
- 147, 148
- 150, 151, 152, 157, 158, 159
- 172, 178
- 182, 183, 184, 187, 188
- 195, 197, 198

**联通号段**
- 130, 131, 132
- 145, 146
- 155, 156
- 166, 167
- 171, 175, 176
- 185, 186
- 196

**电信号段**
- 133, 149, 153
- 173, 174, 177
- 180, 181, 189
- 190, 191, 193, 199

### 用户需要开通运营商邮箱

**重要提示**: 手机号用户需要先开通对应的运营商邮箱服务，才能接收邮件。

- **移动用户**: 发送短信 `KT` 到 `10086` 开通139邮箱
- **电信用户**: 发送短信 `KT` 到 `10001` 开通189邮箱
- **联通用户**: 发送短信 `KT` 到 `10010` 开通沃邮箱

---

## 常见问题

### 1. 邮件发送失败怎么办？

**可能原因：**
- SMTP授权码配置错误
- 163邮箱未开启SMTP服务
- 网络连接问题
- Supabase环境变量未正确设置

**解决方法：**
- 检查Supabase Dashboard中的环境变量配置
- 确认授权码正确(不是邮箱密码)
- 查看Edge Function的日志: `supabase functions logs send-reset-password-email`
- 测试SMTP连接是否正常

### 2. 手机号用户收不到邮件

**可能原因：**
- 用户未开通运营商邮箱服务
- 手机号号段未被识别
- 邮箱地址错误

**解决方法：**
- 提醒用户开通运营商邮箱(详见上文)
- 检查手机号是否在支持的号段列表中
- 查看Edge Function日志确认目标邮箱地址

### 3. 页面显示临时密码

**原因：**
这是一个容错机制。当邮件发送失败时，系统会在页面上显示临时密码，确保用户仍能重置密码。

**正常情况：**
邮件发送成功时，页面不会显示临时密码，只会提示"密码已发送到您的邮箱"。

### 4. 163邮箱每日发送限制

163免费邮箱有每日发送邮件数量限制(通常为50-200封)。

**解决方法：**
- 监控每日发送数量
- 如果超出限制，考虑使用企业邮箱或专业邮件服务(如SendGrid、阿里云邮件推送等)

### 5. SSL/TLS连接错误

**错误信息**: "SSL handshake failed"

**解决方法：**
- 确保 `SMTP_PORT` 设置为 `465`(SSL) 或 `25`(非加密)
- 163邮箱推荐使用端口 `465` 和SSL连接

### 6. Edge Function调用超时

**原因：**
SMTP连接和发送邮件可能需要较长时间。

**解决方法：**
- 检查网络连接
- 考虑使用异步邮件队列(如果邮件量大)

---

## 邮件模板

当前邮件使用的文本模板如下：

```
尊敬的用户,您好!

您的临时密码为：[临时密码]

请使用此临时密码登录系统,登录后请立即修改密码以确保账户安全。

温馨提示:
1. 此临时密码仅用于本次登录
2. 请妥善保管您的密码,不要告诉他人
3. 如非本人操作,请立即联系客服

此邮件由系统自动发送,请勿直接回复。

记账宝团队
[发送时间]
```

如需修改模板，请编辑文件:
```
supabase/functions/send-reset-password-email/index.ts
```

在第105-125行修改邮件内容。

---

## 安全建议

1. **保护授权码**
   - 不要将授权码提交到代码仓库
   - 仅在Supabase环境变量中配置
   - 定期更换授权码

2. **监控邮件发送**
   - 定期检查Edge Function日志
   - 监控异常发送行为
   - 设置发送频率限制

3. **邮件内容安全**
   - 临时密码应该是随机生成的
   - 邮件中不要包含敏感信息
   - 提醒用户登录后立即修改密码

4. **防止滥用**
   - 考虑添加验证码
   - 限制同一账号的重置频率
   - 记录重置日志

---

## 进一步改进

1. **使用HTML邮件模板**
   - 更美观的邮件样式
   - 添加公司Logo和品牌元素

2. **支持更多邮件服务商**
   - QQ邮箱(smtp.qq.com)
   - Gmail(smtp.gmail.com)
   - 企业邮箱

3. **短信验证码**
   - 对于手机号用户，使用短信发送临时密码
   - 集成阿里云短信、腾讯云短信等服务

4. **邮件队列**
   - 异步处理邮件发送
   - 提高系统响应速度
   - 支持重试机制

---

## 联系支持

如有问题，请联系技术支持或查看：
- Supabase文档: https://supabase.com/docs/guides/functions
- Deno SMTP库: https://deno.land/x/smtp@v0.7.0

---

**最后更新**: 2026-02-24
